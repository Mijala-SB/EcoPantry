package com.ecopantry.app.data

import androidx.room.ColumnInfo
import androidx.room.Entity
import androidx.room.PrimaryKey
import com.ecopantry.app.model.NotificationType
import java.util.UUID

/** In-app notification record shown on the Notifications screen (FR11, FR12, US 4.1-4.3). */
@Entity(tableName = "notification_table")
data class NotificationTable(
    @PrimaryKey
    @ColumnInfo(name = "notification_id")
    val notificationId: String = UUID.randomUUID().toString(),
    @ColumnInfo(name = "user_id")
    val userId: String,
    @ColumnInfo(name = "type")
    val type: NotificationType,
    @ColumnInfo(name = "title")
    val title: String,
    @ColumnInfo(name = "message")
    val message: String,
    @ColumnInfo(name = "related_item_id")
    val relatedItemId: String? = null,
    @ColumnInfo(name = "is_read")
    val isRead: Boolean = false,
    @ColumnInfo(name = "timestamp")
    val timestamp: Long = System.currentTimeMillis()
)
